
    <%- include('header'); -%>
    <h1>Home</h1>
    <p><%=homeStartingContent %></p>
    <% posts.forEach(function(post){ %>
        <h1><%=post.title[0].toUpperCase() + post.title.substring(1) %></h1>
        <p><%=post.content %></p>

    <% }) %>
    <%- include('footer'); -%>

</html>